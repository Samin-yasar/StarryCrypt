// Re-export everything from the new translations module for backwards compatibility
export { getTranslations, LANGUAGES } from '@/translations';
export type { Lang, Translations } from '@/translations';
